{
  "name": "figmo",
  "version": "0.1.0",
  "description": "CLI for the Figmo design system platform",
  "license": "MIT",
  "homepage": "https://figmo.dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/user-unkn/figmo-cli.git"
  },
  "bin": {
    "figmo": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsup": "^8.4.0",
    "typescript": "^5"
  }
}
